---
"@moonshot-ai/kimi-code": patch
---

Allow file-defined subagents to opt into an exact model and thinking effort. Set `model` and `thinking_effort` in an agent file while enabling the experimental agent-profile model binding flag.
