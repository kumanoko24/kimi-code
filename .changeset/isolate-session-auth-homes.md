---
'@moonshot-ai/kimi-code': patch
---

Add opt-in session and OAuth home overrides for isolated CLI profiles. Set `KIMI_CODE_SESSION_HOME` and `KIMI_CODE_AUTH_HOME` to share history and managed credentials without sharing the profile's config.
